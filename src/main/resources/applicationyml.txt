spring:
  datasource:
    url: "jdbc:mysql://localhost/login"
    username: "root"
    password: "M!ehddnr0516"
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      #      스프링 시작할 때 DB에 있는 테이블을 어떻게 처리할지 매핑이 잘못되었을 때 어떻게 할것인가
      #      create : 기존 테이블이 있다면 삭제 후 다시 생성
      #      create-drop : 스프링이 종료될 때 테이블을 모두 제거
      #      update : 객체와 테이블이 다른 부분만 변경
      #      validate : 객체와 테이블이 동일한지 확인
      #      none : 별다른 조치를 하지 않는다.
      ddl-auto: create
    properties:
      hibernate:
        #        JPA를 사용해 DB에 SQL을 날릴 때 SQL을 보여줄 것인가
        show_sql: true
        #        SQL을 보여줄 때 예쁘게 포맷팅 할 것인가
        format_sql: true
        #        이 옵션으로 DB를 특정하면 조금씩 다른 QQL을 수정해준다
        dialect: org.hibernate.dialect.MySQL8Dialect